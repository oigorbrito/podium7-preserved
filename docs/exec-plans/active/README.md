# Active execution plans

Place one Markdown plan here for each non-trivial mission currently in progress. `docs/CURRENT-WORK.md` must point to the active plan and remain a short current-state view rather than a running history.

When the mission is integrated, move its final record to [`../completed/`](../completed/) and set `CURRENT-WORK.md` to the next mission or `Status: none`.
