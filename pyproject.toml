[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "podium7"
version = "0.1.0"
description = "Evidence-driven automotive knowledge acquisition, integration, reconciliation, and export system."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
pbev = ["pdfplumber>=0.11,<1"]

[tool.setuptools]
packages = ["podium7"]
